---
import { ArrowLeft } from "lucide-react";
export interface Props {
  displayBackButton: boolean;
}

const { displayBackButton } = Astro.props;
---

<section class="flex h-1 px-4 py-6 md:sticky md:top-0">
  {
    displayBackButton && (
      <a href="/">
        <ArrowLeft className="w-10 h-10 p-2 rounded-full hover:bg-gray-100" />
      </a>
    )
  }
</section>

<header>
  <section class="content">
    <div class="flex items-end justify-between">
      <a href="/" class="title">mnml</a>
    </div>
  </section>
</header>
<style>
  @reference "@/styles/global.css";

  .content {
    @apply mx-auto w-full max-w-screen-sm px-6 sm:px-10;
    @apply pt-8;
  }

  .title {
    font-family: "Sacramento", cursive;
    @apply text-5xl;
    @apply text-zinc-700;
    @apply hover:text-zinc-500;
  }

  .link {
    @apply text-blue-600 hover:text-gray-700;
  }
</style>
