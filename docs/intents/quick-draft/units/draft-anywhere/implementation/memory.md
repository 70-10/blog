## Interpretations

- 2026-08-13T09:34:45Z 「編集画面（自動保存）」に本文の装飾やプレビューを含めなかった; `03_units.md` の Unit 1 は編集画面までしか含まず、プレビューは Unit 3 の担当。素の `textarea` にした
- 2026-08-13T09:34:45Z 更新のとき `createdAt` を画面から受け取らず GitHub から読み直した; 「画面を信じない」を `createdAt` にも当てた。1 回の保存で取得と書き込みの 2 回になるが、自動保存は待ちを入れているので回数は問題にならない

## Deviations

- 2026-08-13T09:34:45Z T0（土台の確認）を実装計画より先に実施した; スキルの手順は計画 → ゲート → 実装だが、`test-cases.md` が T0 を「実装の最初にこれをやる」と定めていて、その結果が計画の内容（`packages/admin/vitest.config.ts` を作るかどうか）を左右する。先に確かめて、結果を計画に書いた
- 2026-08-13T09:34:45Z テストが 44 件の予定に対して 46 件になった; 実装中に「保存済みの鍵を使い回す」「更新で `createdAt` を保つ」の 2 件を足す価値があると分かった
- 2026-08-13T09:34:45Z 計画に無いファイルを 2 つ作った; `functions/lib/validate.ts`（検査を 1 か所に集める）と `src/globals.d.ts`（`pnpm typecheck` が `IS_REACT_ACT_ENVIRONMENT` の型で落ちた）
- 2026-08-13T09:34:45Z 依存を一度 `^` 付きで足してしまい、`-E` で入れ直した; `~/.claude/rules/coding/dependency-management.md` が版の固定を求めていて、このリポジトリの既存の `package.json` も固定している

## Tradeoffs

- 2026-08-13T09:34:45Z `jose` の `createRemoteJWKSet` に `cooldownDuration: 30_000` を明示した; 既定のままにする案と、0 にして即時に取り直す案と比べた。0 にすると知らない鍵 ID を送りつけられるたびに取得が走り、外から何度でも起こせる。鍵の入れ替わりは 6 週に 1 回なので 30 秒で十分追いつく
- 2026-08-13T09:34:45Z 画面のテストを testing-library を入れずに書いた; 入れる案と比べた。`react-dom/client` と `act` で足りたので依存を増やさなかった。入力を起こすところだけ手数が増える
- 2026-08-13T09:34:45Z Base64 の変換を自前で書いた; `atob` / `btoa` をそのまま使う案と比べた。あれは latin1 なので日本語が壊れる。`TextEncoder` / `TextDecoder` を挟んだ
- 2026-08-13T09:34:45Z GitHub の 422 も衝突として扱った; 409 だけを見る案と比べた。Contents API は `sha` が合わないとき 422 を返すことがあり、409 だけ見ていると取りこぼす。取りこぼすと**黙って上書きする**ことになる

## Open questions

- 2026-08-13T09:34:45Z 一覧が 1 件ずつ取りに行く回数（N+1）は、下書きが増えると効いてくる; Unit 1 では受け入れた。Unit 4 が冒頭の抜粋つきの一覧に育てるときに見直す
- 2026-08-13T09:34:45Z `packages/admin` のビルドとデプロイの設定（ビルドコマンド・出力先）はリポジトリに入れていない; Cloudflare のダッシュボードで設定する。`pnpm --filter @blog/admin build` で `packages/admin/dist` に出ることは確かめた
