## Interpretations

- 2026-08-13T05:15:31Z `getPosts()` を「下書きを公開対象から外す唯一の口」と読んだ; `grep` で確かめたところ本番コードで `getCollection("posts")` を呼ぶのは `src/lib/repositories/posts.ts` だけで、5 つの生成物（一覧・記事ページ・タグページ・RSS・OGP 画像）がすべて `getPosts()` を通る。記事本文の Markdown 内に `getCollection` の記述があるがコード例であり実行されない
- 2026-08-13T05:15:31Z 「下書きを posts コレクションに入れるなら」を条件付きで書いた; Q1（下書きの形）が未決のため、変更対象のファイルを断定せず条件付きで並べた。断定すると design の選択を explore が先取りしてしまう

## Deviations

- 2026-08-13T05:15:31Z Q1（下書きをどんな形でリポジトリに持つか）を explore で決めなかった; unattended.md は「既存のコード・docs/rules・上流の成果物から決められるなら確認せずに決めて進む」と書くが、これは design の担当（`artifact-schemas.md` の Stage 2 が `data-model.md` を持つ）。explore で決めると同じ判断が design で書き直され、2 か所に同じ事実が残る。選択肢と分かれ目だけを observation として置き、選択は design に残した
- 2026-08-13T05:15:31Z `glossary-drift.sh` の指摘（「メモ」→「下書き」）を、統一語への置き換えではなく言い換えで解消した; 検出された箇所は ADR 0006 の却下した代替案の名前を引いたもので、下書きの別名として使っていたのではない。ただし検出は部分一致なので、語自体を使わない書き方に変えた

## Tradeoffs

- 2026-08-13T05:15:31Z Q2（管理画面をどこで動かし認証をどう作るか）を `[B]` で人に返した; 続ける案（構成を AI が選んで design へ進む）と比べた。選ばなかったのは 2 点。①公開 URL と認証方式は外部に見える約束で、覆すと画面・裏側・テストの全部が作り直しになる ②どの案を採ってもアカウント作成・アプリ登録・秘密の値の登録は AI にできず、構成だけ先に選んでも implement が用意できない値の前で止まる。unattended.md の中断条件 1（仕様に関わる不明点）と 2（Unit のスコープ外の変更）の両方に当たる
- 2026-08-13T05:15:31Z 中断する前に explore の 4 成果物を書き切った; 疑問が出た時点で止める案と比べた。Q2 が決まっても既存コードの制約（`getPosts()` の 1 か所・スキーマの必須・CI のロックファイル検査・テストの既定の拾われ方）は変わらないので、書いた分は再実行でそのまま使える

- 2026-08-13T05:26:00Z この memory.md 自体が `glossary-drift.sh` に当たり続ける; 上の Deviation が検出された避ける別名を名指ししているため。名前を伏せると記録の意味が失われるので残した。書いたルール（`project.md` の「記録」）の「置き換えが誤りになる場合」に当たる

- 2026-08-13T05:55:00Z Q2 が解けたので `[B]` を `[ ]` に戻して再実行するのではなく、explore の成果物を直して `[x]` にした; `execution-plan.md` の「止まったとき」は戻して再実行と書くが、あれは別セッションで再開する前提。中断したのはステージの途中ではなく疑問の解消の段で、4 成果物は書き上がっていた。戻すと同じ調査をやり直すだけになる
- 2026-08-13T05:55:00Z `blocked.md` を消した; 中断が解けたので残すと progress と食い違う。`execution-plan.md` の成果物の表も「`[B]` で終わったときだけ」と書いている。案の比較は ADR 0008 の却下した代替案に、確かめた事実は `constraints.md` の「管理画面の土台」に移した。元の内容はコミット `ffc641e` に残っている

## Open questions

- 2026-08-13T05:15:31Z Q3（画面のテストをどの環境で走らせるか）は Q2 に従属する; `vitest.config.ts` の `environment` は現在 `node` の 1 つだけ。画面をどの枠組みで作るかが決まらないと決められない。Stage 3（test-design）で決める
- 2026-08-13T05:26:00Z `docs/recording-conventions.md` の「ルールの置き場」に食い違いがある; 「既定は `project.md`」と書いた直後に「狭いほうを既定にするのは…」と続くが、`project.md` は 3 つのうち一番広い。`project.md` 自身も「昇格の既定はこのファイル」と書いているので既定は `project.md` として扱った。どちらが意図かは書いた人にしか分からないので、直さずに報告する
